<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SuitmediaProjectTest" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/blue_color</item>
        <item name="android:textColor">@color/dark_text</item>
    </style>

    <style name="AppCompatAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="fontFamily">@font/poppins_regular</item>
    </style>

    <style name="Theme.SuitmediaProjectTest" parent="Base.Theme.SuitmediaProjectTest" />
</resources>